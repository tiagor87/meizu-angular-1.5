'use strict';

describe('shoppingcart module', function() {

  beforeEach(module('myApp.shoppingcart'));

  describe('shoppingcart controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var shoppingcartController = $controller('shoppingcartController');
      expect(shoppingcartController).toBeDefined();
    }));

  });
});